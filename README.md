# Genlayer-neural-consensus-spinner
Here is a "Neural Consensus" style version that aligns with the GenLayer spirit: 8 points (representing validator nodes) A "pulse" gradient moves around the circle (simulating the voting/consensus process) The central logo/mark remains static (does not rotate) Pure SVG + CSS; very lightweight Colors follow the official branding
